---
context: research
phase: (none - Ra STT engine swap + continuous-hearing bug, plus omi workstream in Ra-omi)
task: 9
total_tasks: 12
status: blocked
last_updated: 2026-09-17T18:47:17Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Continuous (always-on) hearing is DEAD in the new sherpa exe while push-to-talk is PERFECT.** User by-ear test (2026-09-18): "voice works perfectly in the ptt mode but in the continuous mode it ain't hearing shit or doing anything… I said the wake word, it heard it in ptt but in continuous it ain't hearing shit, the mic works completely fine too." PTT and continuous use the SAME `_get_sherpa()` recognizer and `_open_capture_source()` backend — so capture + model + recognizer are all PROVEN working. The broken link is `_SherpaStream` phrase/wake delivery (audio_io.py:1477-1634). This is THE top priority: fix it, understand it, and make it perfect by ear before this week ends (user's explicit mandate — "by any means, even if it takes the whole tech stack, code or file rebuilt"). Do NOT claim victory without the by-ear gate.
- [ ] CONSTRAINT: **`RA_STT_DEBUG` is set in NO env file.** Project `.env` and `~/.ra/.env` both lack ALL `RA_STT_*` keys. The new exe therefore writes ZERO lines to `~/.ra/ra_stt_debug.log` — every diagnostic run up to now was blind. The 18:53 log is in the OLD `floor=` format (pre-sherpa binary); the new `fixed_thr=` format has NEVER run. Set `RA_STT_DEBUG=1` in `~/.ra/.env` (running exe) AND project `.env` (rebuilds copy it via build_exe.bat) before any diagnostic run, and treat the debug log as the primary evidence source.
- [ ] CONSTRAINT: **Wake-word detection lives ONLY on final phrases.** `_on_phrase` (assistant.py:405-429) is the ONLY place wake words are checked; `_on_partial` (assistant.py:432-433) just posts "…" to the HUD. If the stream never emits a final phrase, the wake word can never wake Ra — exactly the reported symptom. Any fix must add wake detection on PARTIALS plus a phrase boundary that does NOT depend on sherpa's endpoint rules.
- [ ] CONSTRAINT: **Sherpa streaming endpoint rules are the ONLY phrase trigger and they never fire on this rig.** `_get_sherpa()` (audio_io.py:670-682) enables endpoint detection with `rule1_min_trailing_silence=2.4`, `rule2_min_trailing_silence=1.2`. In continuous mode a phrase is dispatched ONLY when `is_endpoint()` turns true (audio_io.py:1594). PTT never uses endpoints — `_transcribe_sherpa` (audio_io.py:1931) calls `input_finished()` instead. Prime root-cause hypothesis: with a fan/hum ambient floor, sherpa's trailing-silence requirement is never satisfied → zero phrases → zero wake. Verify with the debug log, then go gate-driven.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Using sherpa's own endpoint detection as the SOLE phrase boundary in always-on mode | Streaming endpoint needs 1.2-2.4s of sherpa-judged silence; on a noisy laptop floor it never fires → zero phrases while PTT (`input_finished`) works. Continuous heard literally nothing in the user test. | blocking | Drive phrase boundary from Ra's OWN gate (`_classify_speech` + `_track_noise_floor` + trailing-quiet via `STT_PAUSE_THRESHOLD`), mirroring `_WhisperVADStream._process/_finalize_utterance` (audio_io.py:1753-1814). Treat sherpa `is_endpoint()` as a best-effort secondary trigger, never the sole one. |
| Checking wake words only on final {@code on_phrase} | If no phrase ever completes (see row above) the wake word is never heard. | blocking | Add wake-word check on `on_partial` in assistant (fire immediately when a partial contains a wake word), keep `_on_phrase` as the robust fallback so currently-non-wake follow-ups still flow. |
| Running hearing diagnostics without block-level logging | Neither .env sets `RA_STT_DEBUG`; the exe logged nothing after 22:06. "Fixed it" was previously claimed while hearing was actually broken (00:47 build) because proof was missing. | blocking | `RA_STT_DEBUG=1` in BOTH .env files; check `~/.ra/ra_stt_debug.log` after EVERY run before drawing conclusions. |
| `_SherpaStream` uses a FIXED `STT_ENERGY_THRESHOLD` gate (audio_io.py:1574) while MicSession/WhisperVADStream use the adaptive `_classify_speech` floor | Divergent hearing logic between the three streams; the sherpa path abandons the adaptive noise-floor it was built on. Unknown if the fixed gate causes missed speech at soft volumes. | advisory | If post-endpoint-fix hearing is still uneven, unify on `_classify_speech` + `_track_noise_floor` so all three streams share one gate. |
| Loosened gate knobs without by-ear validation | `STT_MIN_SPEECH_SECONDS` 0.4→0.15, `STT_MODULATION_THRESHOLD` 0.10→0.05, `STT_NOISE_RATIO` 1.8→1.0 (config.py) — higher fan false-positive risk in exchange for catching softer speech. | advisory | Cancel any knob that lets the fan get through during the by-ear acceptance run. |
| Old frozen-exe "digital silence" was blamed on device pick only | PTT now proves capture + device work (old picker fix was valid). Do NOT re-diagnose device selection; focus on the stream's phrase pipeline. | advisory | Verify capture separately only if the debug log shows zero `vol=S` lines with `RA_STT_DEBUG=1`. |

_Remove rows that do not apply._

<current_state>
Ra repo at `C:\Users\mdahm\OneDrive\Documents\Ra` (git HEAD `02da0f0`, working tree DIRTY with the full sherpa engine swap — left uncommitted BY USER CHOICE this pause).

**What was built this session (Track A, uncommitted):**
- sherpa-onnx 1.13.8 installed on BOTH interpreters (dev pythoncore-3.14-64 and Store 3.13 build/test env).
- Both model bundles downloaded + extracted into `~/.ra/models/`: `sherpa-onnx-nemo-streaming-fast-conformer-transducer-en-480ms-int8` (ONLINE, live partials) and `sherpa-onnx-nemo-parakeet-tdt-0.6b-v2-int8` (OFFLINE, parakeet final refinement). `.tar.bz2` originals kept.
- config.py (dif +55): `STT_ENGINE=sherpa-onnx` default, `STT_FINAL_ENGINE=parakeet` default (options sherpa/whisper/groq/off), `SHERPA_ONNX_MODEL_*`, `SHERPA_OFFLINE_MODEL_*`, `STT_DENOISE`, `GROQ_WHISPER_MODEL/URL`; wake words widened to "fire up, ra, hey ra, rah, raw, ray, rad, rock"; gate knobs loosened.
- audio_io.py (dif +540, now 2016 lines): `_download_sherpa_model`, `_download_sherpa_offline_model`, `_extract_sherpa_tar`, `_get_sherpa` (online, endpoint rules), `_get_sherpa_offline` (OfflineRecognizer nemo_transducer feature_dim=128), `_SherpaStream` (feeds EVERY block; partials live; phrase at `is_endpoint`; `_fin_q` worker re-finalizes via parakeet/groq/whisper → `on_correct`); `_groq_transcribe`; `_transcribe_sherpa` (+`_offline`), `start_mic` dispatch (sherpa → vosk → whisperVAD), `listen_once` chain.
- assistant.py (dif +30): boot banner `_get_sherpa()`; `_wait_correction` gated to whisper/groq finals only.
- hud/ra.spec (dif +10): `sherpa_onnx` via `collect_all` (binaries/data/submodules).
- test_voice_pipeline.py (dif +111): `_FakeRecognizer`/`_FakeStream` tests for `_SherpaStream` (partial dispatch, noise-blip drop, parakeet fin-queue).
- README + test_branding updated; `.env.example` documents the new STT env.
- Tests: **224 passed** (ran on Store 3.13 at 00:10 this session, `-x -q`).
- exe REBUILT: `hud\dist\Ra.exe` 2026-09-17 22:06 local, 339.2 MB, sherpa_onnx confirmed bundled (build_log.txt L354, L402).

**The failure (by-ear, user, 2026-09-18):** new exe continuous mode hears NOTHING; PTT perfect. See BLOCKING CONSTRAINTS 1-4.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Root-cause + fix mic device auto-pick on old exe — Done (committed `feff3ce`; PTT now proves capture works)
- Task 2: STT research → sherpa-onnx hybrid (online fast-conformer partials + offline parakeet final) — Done
- Task 3: Install sherpa-onnx 1.13.8 on dev + Store 3.13 — Done
- Task 4: Download + extract BOTH sherpa model bundles to ~/.ra/models — Done
- Task 5: Clone omi (blobless+sparse, Temp\opencode\omi) + Ra-omi worktree baseline — Done
- Task 6: Implement STT engine layer (config/audio_io/assistant/ra.spec) — Done (UNCOMMITTED)
- Task 7: Tests (224 pass on Store 3.13) — Done
- Task 8: Rebuild exe with sherpa (22:06, 339MB) — Done
- Task 9: Fix continuous-hearing in new exe — IN PROGRESS (blocked) ← WE ARE HERE
</completed_work>

<remaining_work>

- Task 9 (NOW, top priority, deadline = end of this week): Make continuous hearing work. Diagnostic playbook:
  1. Set `RA_STT_DEBUG=1` in `~/.ra/.env` AND project `.env` (no RA_STT_* keys exist anywhere right now).
  2. Run the current exe, say the wake word; open `~/.ra/ra_stt_debug.log` and CLASSIFY:
     - NO lines at all → mic stream never started (check HUD boot banner / ralog; does `start_mic` error?).
     - `vol=S … fixed_thr=… -> SPEECH` lines present but NO phrase ever → endpoint never fires (prime hypothesis; gate-driven fix below).
     - Phrase fires in log but assistant silent → assistant-level `_on_phrase` drop (`_is_echo` / wake-match / busy).
  3. Dev smoke test on pythoncore-3.14-64: tiny script driving a REAL `_SherpaStream` (or `_get_sherpa` recognizer directly) with actual mic + a synthetic speech WAV; confirm partials and `on_phrase` fire with the real recognizer (the code tests use a FAKE recognizer — nobody has smoke-tested the real one end-to-end).
  4. Fix per root cause. If endpoint is the blocker: replace sherpa endpoint as sole boundary with Ra's own gate-driven endpoint — mirror `_WhisperVADStream` (`_process` L1754-1786: `_track_noise(level,block)` + `_classify_speech`, speech start → clip; trailing quiet ≥ `STT_PAUSE_THRESHOLD` → `_finalize_utterance` L1788-1814); keep feeding sherpa every block for live partials, add a `reset` after own-boundary finalize; ALSO add wake-word check on partials in `_on_partial`.
  5. pytest on Store 3.13 (expect 224 green; add a regression test for the own-boundary path) + dev live-capture.
  6. Rebuild exe; user by-ear acceptance checklist: normal speech heard, follow-up WITHOUT wake word works, fan ignored, scream heard, PTT still perfect.
- Task 10: By-ear final verification of the fixed exe (user is the gate — NEVER claim "fixed" without it).
- Task 11: Sync the verified sherpa engine fix into Ra-omi worktree (it is a Ra fix, per repo-split rule).
- Task 12: Track B — omi pillars B1 memory / B2 apps / B3 personas / B4 mcp / B6 HUD (ALL work in `C:\Users\mdahm\OneDrive\Documents\Ra-omi`, never here).
</remaining_work>

<decisions_made>

- sherpa-onnx = new default STT. Hybrid design: ONLINE fast-conformer streams LIVE partials; OFFLINE parakeet (nemo_transducer, feature_dim=128) refines each final in a background worker (`on_correct` reports upgrades). `STT_FINAL_ENGINE` default `parakeet`; options sherpa/whisper/groq/off. vosk + whisperVAD remain fallback engines.
- Assistant waits for whisper/groq finals only (`_wait_correction`); parakeet finals arrive via `on_correct` (assistant.py `_corrections` path) so responses stay instant.
- Wake words expanded: "fire up, ra, hey ra, rah, raw, ray, rad, rock" (config.py:274).
- Gate knobs loosened for softer speech (config.py): `STT_MIN_SPEECH_SECONDS` 0.15, `STT_MODULATION_THRESHOLD` 0.05, `STT_NOISE_RATIO` 1.0.
- Repo split stays: THIS Ra folder remains the pre-omi baseline (nothing omi lands here); the sherpa swap is Ra's own fix and later syncs into Ra-omi. Omi pillars happen ONLY in Ra-omi.
- User chose NOT to commit this pause → working tree stays dirty (all Track A implementation uncommitted).
- Exe rebuild rules: kill Ra first; build_exe.bat copies project .env → ~/.ra/.env.
</decisions_made>

<blockers>

- Continuous hearing dead on new exe (PTT perfect) — active blocker; hypotheses + fix in constraints above.
- Debug-blind: `RA_STT_DEBUG` unset everywhere — set it before any further run.
- Subagent/task tooling was broken last session (`session_message.seq` NOT NULL); unverified this session. Do diagnostic work INLINE if it still fails.
</blockers>

## Required Reading (in order)

1. `src/ra/audio_io.py` — focus regions: `_SherpaStream` L1477-1634 (`_process_block` L1568-1595 gate + endpoint; `_end_phrase` L1597-1613), `_get_sherpa` L647-683 (endpoint rules), `start_mic` L1834-1875, `_transcribe_sherpa` L1931-1953 (PTT, works), `_WhisperVADStream._process/_finalize_utterance` L1753-1814 (the gate-driven reference fix), `_classify_speech/_track_noise_floor` near L1171-1217, `_is_valid_speech` L1242-1253.
2. `src/ra/assistant.py` — `_on_phrase` L405-429 (wake = final phrase only), `_on_partial` L432-433 (wake MUST be added here), `_mic_worker` L436-461.
3. `src/ra/config.py` — STT section L454-549; wake words L270-275; `CONTINUOUS_LISTEN` default on L540.
4. `~/.ra/ra_stt_debug.log` — partial evidence, OLD format (18:53); re-collect after setting `RA_STT_DEBUG=1`.
5. `hud/build_log.txt` — proves sherpa_onnx bundled into Ra.exe (L354, L402); `hud/ra.spec` for the bundle config.
6. `tests/test_voice_pipeline.py` — new `_FakeRecognizer` sherpa tests; extend for the own-boundary path.
7. `.planning/HANDOFF.json` — structured state mirror.

## Critical Anti-Patterns (do NOT repeat these)

- **Trusting sherpa `is_endpoint()` as the only phrase trigger in always-on mode** → went a whole by-ear session silent. Use Ra's own gate-driven phrase boundary; wake on partials.
- **Diagnosing without `RA_STT_DEBUG=1`** → the 22:06 exe produced zero evidence. Instrument first.
- **Claiming hearing "fixed" without the user by-ear gate** → happened at the 00:47 build; user remains frustrated. Every hearing change end-counts with a real user run.
- **Full omi clone / adding `docs/` to sparse set** → blob timeouts; keep the 9 sparse dirs.

## Infrastructure State

- Dev interpreter (mic-capable, sherpa 1.13.8): `C:\Users\mdahm\AppData\Local\Python\pythoncore-3.14-64\python.exe`.
- Build/test interpreter (Store 3.13, AppContainer mic-BLOCKED, sherpa 1.13.8): `C:\Users\mdahm\AppData\Local\Microsoft\WindowsApps\PythonSoftwareFoundation.Python.3.13_qbz5n2kfra8p0\python.exe`. Test cmd: `& "<that>" -m pytest -x -q` — **last run 224 passed (2026-09-18 00:10)**.
- Models `~/.ra/models`: vosk-small, streaming fast-conformer 480ms int8, offline parakeet 0.6b int8 (both extracted + .tar.bz2 kept).
- `hud\dist\Ra.exe`: 2026-09-17 22:06 local, 339.2 MB, bundled sherpa-onnx. Hearing: PTT OK, continuous DEAD.
- `RA_STT_*` env: absent from project `.env` and `~/.ra/.env` — SET `RA_STT_DEBUG=1` (and any overrides) before next run.
- omi clone: blobless sparse, `Temp\opencode\omi` (dirs: app backend mcp web desktop plugins sdks config contracts). Ra-omi worktree: `C:\Users\mdahm\OneDrive\Documents\Ra-omi` (own repo, baseline `91d6eab`).
- git: HEAD `02da0f0`; working tree dirty (all Track A uncommitted by user choice). Untracked: `hud/build_log.txt`, `mic_diag_out.txt`.
- No servers running. Ra processes stopped.

## Pre-Execution Critique Required

- Design artifact: `_SherpaStream` (implemented but never verified against a REAL recognizer/real audio).
- Critique focus: (1) byte-level evidence of the continuous failure via `RA_STT_DEBUG` before any code change; (2) sherpa decode pacing (fast-conformer int8 RTF on this CPU) — inline decode per block must keep up or backpressure falls over; (3) whether the gate-driven boundary + partial-wake refactor covers wake, follow-up, barge-in and noise rejection; (4) PTT must stay byte-for-byte untouched.
- Gate: user by-ear acceptance for EVERY hearing change; "fixed" is a user-verifiable state, not a code change.

<context>
User mandate (verbatim frustration): "the continuous hearing issue must be found and fixed by any means, even if it takes the whole tech stack, code or file rebuilt, but it must work harmoniously... the voice works perfectly in the PTT mode but in the continuous mode it ain't hearing shit or doing anything, I said the wake word, it heard it in PTT but in continuous it ain't hearing shit, the mic works completely fine too... make sure it is the first priority and also it must be fixed and understood and made perfect before this week ends."

The STT engine swap (parakeet + streaming conformer + Groq optional) is implemented, tested (224 green), and bundled — but the always-on path is broken end-to-end. Despite huge progress, THE thing the user actually cares about (continuous hands-free hearing from boot) is regressed vs. what vosk provided before. Everything else — omi, Ra-omi sync, models-next-to-exe — is frozen until continuous hearing is user-verified. Be rigorous with evidence (debug log), take the gate-driven boundary route if endpoint theory confirms, and treat the by-ear verification as the single acceptance gate. Be economical: reuse Temp\opencode\omi clones, never re-clone, work inline if subagents are broken.
</context>

<next_action>
Start with (Task 9, role exactly as above): (1) set `RA_STT_DEBUG=1` in `~/.ra/.env` + project `.env`; (2) run the current `hud\dist\Ra.exe`, speak the wake word, classify `~/.ra/ra_stt_debug.log` (no lines / SPEECH-but-no-phrase / phrase-but-assistant-silent); (3) run a REAL-recognizer smoke test on pythoncore-3.14-64; (4) implement the gate-driven phrase boundary (mirror `_WhisperVADStream`) + wake-word detection on partials; (5) pytest on Store 3.13 + dev live-capture; (6) rebuild exe; (7) user by-ear acceptance (speech heard, wake works, follow-up without wake, fan ignored, scream heard, PTT perfect). Then commit (user authorized committing only after this fix), sync into Ra-omi, and only then Track B omi pillars.
</next_action>