---
context: research
phase: (none - STT-engine swap + omi replication workstream)
task: 4
total_tasks: 9
status: in_progress
last_updated: 2026-09-16T19:47:14Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Built Ra.exe still hears wrong even with the correct mic device picked.** Debug log (`Temp\opencode\ra_stt_debug_run1.log`) shows `sd[1] Microphone Array` was auto-picked (probe peak 0.1136), yet during the run every block logged `lvl=0.0000 -> noise` — the frozen exe streamed DIGITAL SILENCE. Device selection was fixed but capture stability on the built exe is NOT solved. Verify whether this recurs only in the frozen exe (dev pythoncore-3.14 captured speech fine earlier) before claiming the engine swap fixed hearing.
- [ ] CONSTRAINT: **Parakeet model download is incomplete.** `~/.ra/models/sherpa-onnx-nemo-parakeet-tdt-0.6b-v2-int8.tar.bz2` is ~79MB (needs ~750MB). User aborted mid-download. DELETE the partial file and re-download with a long timeout before any sherpa work.
- [ ] CONSTRAINT: **Task/subagent tool is failing** (`NOT NULL constraint failed: session_message.seq` — hit twice, both `general` invocations). Do omi research INLINE (clone + read + grep worked fine). Retry subagents only if this clears.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trusting the OS-default/portable backend while the exe runs | Sounddevice's default device + winmm fallback deliver 0 blocks on this rig; fixed by `_pick_mic_device()` probing all inputs + first-block aliveness check | advisory | Keep aliveness verification + `RA_STT_MIC_DEVICE` override; never trust default device |
| Diagnosing hearing only via the in-memory ralog | ralog is memory-only; the HUD log pane was blank → added `~/ra/ra_stt_debug.log` (per-block, behind `RA_STT_DEBUG=1`) | advisory | Use the file log; both `RA_STT_DEBUG=1` in `~/.ra/.env` AND project `.env` (build_exe.bat copies project→~/.ra) |
| Full `git clone` of omi | Timed out at 180s; `--filter=blob:none --sparse` clone worked fast; sparse-checkout of `docs/` then timed out on blob fetch and was dropped | advisory | Keep sparse dirs to: `app backend mcp web desktop plugins sdks config contracts`. Do NOT add `docs` |
| Frozen exe vs dev interpreter divergence | Store 3.13 (build) is AppContainer → cannot open mic; dev capture works on pythoncore-3.14-64 | blocking | Run DEV capture with pythoncore-3.14-64; rebuild exe from Store 3.13 AFTER `pip install sherpa-onnx` on it; verify frozen exe separately |

_Remove rows that do not apply._

<current_state>
Ra repo at `C:\Users\mdahm\OneDrive\Documents\Ra`. Two parallel tracks:

**Track A — STT engine swap (in flight, pre-implementation):**
- sherpa-onnx 1.13.8 installed on dev interpreter `C:\Users\mdahm\AppData\Local\Python\pythoncore-3.14-64\python.exe` (imports fine, `OnlineRecognizer.from_transducer` confirmed: tokens/encoder/decoder/joiner paths, `feature_dim=80`, `sample_rate=16000`, inline endpoint rules `rule1_min_trailing_silence=2.4`, `rule2_min_trailing_silence=1.2`; transpose params — NO EndpointConfig class).
- Model NOT downloaded yet (partial file present, must delete + re-download).
- `config.py`/`audio_io.py` NOT yet edited for sherpa (still default `STT_ENGINE=vosk`).

**Track B — omi replication (research phase):**
- Blobless+sparse clone at `C:\Users\mdahm\AppData\Local\Temp\opencode\omi` (sparse dirs: `app backend mcp web desktop plugins sdks config contracts`).
- Top-levels inventoried: `app/`=Flutter client (ios/android/lib/web), `plugins/`=chat-tools apps (`omi-*-app`: calendar, clickup, github, google-calendar, hacker-news, linear, notion, slack, shopify, wikipedia, ... + `omi-plugin-sdk`, `oauth`, `composio`, `zapier`, `main.py`), `mcp/`=MCP servers (src/, tests/, examples, pyproject), `backend/`=Python LLM gateway (FastAPI routers, `llm_gateway/`, `diarizer/`, `parakeet/`, `nllb_translation/`, `typesense/`, `services/`, `models/`), plus `web desktop sdks config contracts docs`.
- Deep capability reads (memory/personas/voice pipelines) NOT yet done.
</current_state>

<completed_work>

**Done this session:**
- Diagnosed + fixed mic device selection: `_pick_mic_device()` in `audio_io.py`, `_stt_debug()` file logging, `RA_STT_MIC_DEVICE`/`RA_STT_DEBUG` in `config.py`; live capture verified hitting sd[1] at peak 0.711 in DEV. Rebuilt `hud\dist\Ra.exe` (17-09-2026 00:47, 345MB). User then reported it "got dumber and annoying with incorrect hearing" → archived proof to `Temp\opencode\ra_stt_debug_run1.log` (sd[1] picked, then full-run digital silence) and `mic_diag_out.txt` copied to repo root.
- STT research (web): chose sherpa-onnx + parakeet-tdt-0.6b-v2-int8 (k2-fsa official asset, encoder.int8.onnx 652MB, cc-by-4.0) as the new default local engine; Groq Whisper (`whisper-large-v3-turbo`, existing `RA_GROQ_API_KEY`) as optional cloud FINAL pass; vosk+local-whisper retained as fallbacks. Todo list has the full plan.
- Installed sherpa-onnx 1.13.8 (+ sherpa-onnx-core) on pythoncore-3.14-64.
- Cloned omi (blobless+sparse) and inventoried its tree layout.
</completed_work>

<remaining_work>

- Task 4: DELETE `~/.ra/models/sherpa-onnx-nemo-parakeet-tdt-0.6b-v2-int8.tar.bz2` (partial) and re-download (~750MB) — then extract to `~/.ra/models/sherpa-onnx-nemo-parakeet-tdt-0.6b-v2-int8/`.
- Task 5 (STT engine layer): config.py — add `SHERPA_ONNX_MODEL_ID/URL/DIR`, default `STT_ENGINE="sherpa-onnx"`, `STT_FINAL_ENGINE` gains `"groq"` (plus `GROQ_WHISPER_MODEL`, URL hint), optional `STT_DENOISE` (sherpa NoiseReducer/gtcrn, default off). audio_io.py — `_download_sherpa_model()`, `_get_sherpa()`, `_SherpaStream` (mirror MicSession: feed every block float32/32768 to an `OnlineRecognizer` stream, decode while ready, phrase boundary via `recognizer.is_endpoint(stream)` → `reset(stream)`; keep `_classify_speech`/sustained/`_is_human_audio` backstops; sherpa result IS the final [has punctuation] — skip local-whisper re-finalize, use Groq when `STT_FINAL_ENGINE=groq`), wire into `start_mic()` dispatch, add `_transcribe_sherpa` for `listen_once()` (PTT prefers sherpa when engine=default).
- Task 6 (tests+build): `pip install sherpa-onnx` into Store 3.13; run `pytest` (Store 3.13, expect green — suite has a `test_voice_pipeline.py`); live-capture test on pythoncore-3.14; add sherpa-onnx to `hud/ra.spec` (collect-all / native dlls) and ship model next to exe (`hud/dist/models/` preferred over bloating the 345MB onefile); kill Ra before build; rebuild; **user verifies by ear** (normal speech heard, scream heard, fan ignored, PTT + always-on).
- Track B omi (not started): full capability inventory (conversation memory storage/encryption/segmentation, apps/tools invocation, personas, mcp, voice pipeline models) → add/replace mapping → design V2 `src/ra/omi/` package + `RA_OMI_ENABLED` flags → implement pillars B1 memory, B2 apps, B3 personas, B4 mcp, B6 HUD tabs → tests/rebuild/verify.
</remaining_work>

<decisions_made>

- sherpa-onnx parakeet-tdt-0.6b-v2-int8 = new DEFAULT local STT (best free local, far better than vosk-small+whisper-base, has punctuation/caps). Groq Whisper = optional cloud final. vosk / local-whisper stay as explicit fallback engines. `STT_FINAL_ENGINE` options: sherpa(default none)/whisper/groq/off.
- Keep Ra's noise gate untouched (fan-proof + scream-proof) — sherpa consumes the SAME gated pipeline vosk did; phrase boundary from sherpa endpoint rules; keep sustained-speech + `_is_human_audio` utterance backstops.
- Model placement: dev `~/.ra/models`; release next-to-exe `hud/dist/models/` (avoid 1.1GB exe).
- Omi = additive V2, replace only strictly-better (user's rule: "keep Ra as it is, add as new version; don't mess up any shit").
- **REPO SPLIT (NEW)**: THIS `Ra` folder is FROZEN as the pre-omi baseline (committed at `feff3ce`, voice continuous-convo fix + STT/device work). NO Omi code lands here. All Omi integration happens in a separate worktree: `C:\Users\mdahm\OneDrive\Documents\Ra-omi` (own git repo, baseline `91d6eab` = copy of this source tree minus exes/git/.planning, with `INIT.md` documenting purpose + B1-B6 roadmap). Keep both folders in sync only for genuine Ra fixes; do omi work ONLY in Ra-omi.
- Exe build rules: kill Ra first; ensure `.env` carries flags (`RA_STT_DEBUG=1` etc) into `~/.ra/.env` via build_exe.bat.
</decisions_made>

<blockers>

- exe still hears wrongly (digital silence) — root cause on frozen build not yet isolated; verify post-sherpa build.
- model download aborted (user) — must re-run with permission.
- subagent tool broken — work inline.
</blockers>

## Required Reading (in order)

1. `src/ra/audio_io.py` — STT core: `_track_noise_floor`/`_classify_speech`/`_is_human_audio` (gate), `_open_capture_source`/`_pick_mic_device` (device), `MicSession._process_block/_feed/_finalize` (vosk model), `_WhisperVADStream` (whisper model), `start_mic()` dispatch (line ~1451), `listen_once`/`_transcribe_vosk`/`_record_once` (PTT). 1564 lines.
2. `src/ra/config.py` — STT section lines ~456-546 (`STT_ENGINE`, `VOSK_*`, `STT_FINAL_ENGINE`, `WHISPER_MODEL_SIZE`, gate knobs, `STT_MIC_DEVICE`, `STT_DEBUG`); note the `_env()` RA_* naming pattern and `.env` loading.
3. `C:\Users\mdahm\AppData\Local\Temp\opencode\ra_stt_debug_run1.log` — evidence trail: probe rows at 00:49 (sd[1] real peak 0.1136) then run blocks all `lvl=0.0000 -> noise`; proves exe digital silence.
4. Omi clone `C:\Users\mdahm\AppData\Local\Temp\opencode\omi` — trees fetched: `app/`(Flutter), `plugins/`(Node chat-tools), `mcp/`, `backend/`(Python gateway), `web desktop sdks config contracts`. Do NOT add `docs`.
5. `tests/test_voice_pipeline.py` + run command (on Store 3.13): `& "C:\Users\mdahm\AppData\Local\Microsoft\WindowsApps\PythonSoftwareFoundation.Python.3.13_qbz5n2kfra8p0\python.exe" -m pytest -x -q`.

## Critical Anti-Patterns (do NOT repeat these)

- **Trusting default recording device / portable fallback** → always `_pick_mic_device()` + aliveness check + `RA_STT_MIC_DEVICE` override.
- **Debugging hearing via in-memory ralog** (blank HUD pane) → use `~/.ra/ra_stt_debug.log`.
- **Full omi clone / adding `docs/` to sparse set** → timeouts. Keep the 9 sparse dirs above.
- **Frozen-exe assumption == dev behavior** → Store 3.13 (AppContainer) has no mic; verify capture in dev on pythoncore-3.14, verify frozen exe separately.

## Infrastructure State

- Dev interpreter (mic-capable, HAS sherpa-onnx 1.13.8): `C:\Users\mdahm\AppData\Local\Python\pythoncore-3.14-64\python.exe`.
- Build/test interpreter (Store, AppContainer, mic-BLOCKED, NEEDS `pip install sherpa-onnx`): `C:\Users\mdahm\AppData\Local\Microsoft\WindowsApps\PythonSoftwareFoundation.Python.3.13_qbz5n2kfra8p0\python.exe`.
- Models dir `~/.ra/models`: vosk small (unpacked + zip) + PARTIAL parakeet tar.bz2 (79MB, delete).
- `hud\dist\Ra.exe` = last build 17-09-2026 00:47, 345MB, contains device-pick fix.
- omi clone: blobless sparse, dirs above.
- **THIS repo frozen at baseline** `feff3ce` (all today's STT fixes committed). `.planning/` (this file + HANDOFF.json) committed as WIP on top.
- **Ra-omi worktree** at `C:\Users\mdahm\OneDrive\Documents\Ra-omi`: own git repo, `91d6eab` baseline copy + `e71b8e9` INIT.md. This is where ALL omi work happens. Rebuild exes there with its `build_exe.bat` after copying user env into its `~/.ra` usage (same flag propagation rule).
- No servers running. Ra processes stopped.
- `gsd-sdk` NOT on PATH (worked around with manual timestamp + git commit).

## Pre-Execution Critique Required

- Design artifact: `_SherpaStream` integration plan (modeled on MicSession, endpoint rules for phrase boundary, Groq final optional)
- Critique focus: (1) decode pacing — parakeet 0.6b int8 RTF on this CPU, inline decode on mic worker may lag vs feed; if RTF>1, drop to utterance-batch decoding like `_WhisperVADStream`. (2) digital-silence bug on the built exe — confirm capture actually delivers before blaming the engine. (3) whether sherpa stream should also be gated so fan hum never reaches the recognizer (it has no embedded VAD in streaming mode unless SileroVad used).
- Gate: user by-ear verification is the acceptance gate for EVERY hearing change.

<context>
User's standing directive: "find something better than vosk+whisper which is free → best & local as default, deepgram-alternative optional" AND "replicate all omi capabilities into Ra as a new version, keep Ra as-is, replace only what omi does better." User is frustrated (hearing regressed on the built exe). Two-track plan: finish STT engine swap first (parakeet + optional Groq Whisper) — happens HERE in this Ra repo (it IS Ra's own fix, pre-omi) — then omi pillars (memory/apps/personas/mcp/HUD) which happen in the SEPARATE `Ra-omi` folder. THIS folder stays a pure pre-omi baseline: once the STT engine swap is verified, sync only that engine work into Ra-omi (as a Ra fix, not omi). Subagent infra is broken → all research inline. Be economical with context: reuse the omi clone local reads, don't re-clone.
</context>

<next_action>
Track A (HERE, Ra): (1) delete the partial `~/.ra/models/sherpa-onnx-nemo-parakeet-tdt-0.6b-v2-int8.tar.bz2`; (2) re-download the full model with `curl.exe -L --fail --retry 3` (long timeout, ~750MB); (3) extract into `~/.ra/models/sherpa-onnx-nemo-parakeet-tdt-0.6b-v2-int8/`; (4) add sherpa/Groq constants to `config.py`; (5) implement `_download_sherpa_model`/`_get_sherpa`/`_SherpaStream`/dispatch/`_transcribe_sherpa` in `audio_io.py`; (6) pytest on Store 3.13 + live-capture on pythoncore-3.14; (7) `pip install sherpa-onnx` on Store 3.13, update `hud/ra.spec`, rebuild exe, user by-ear verify.
Track B (in `Ra-omi`): then inventory omi capabilities (clone already at Temp\opencode\omi), port pillars B1 memory / B2 apps / B3 personas / B4 mcp / B6 HUD inside the Ra-omi worktree only; never touch the Ra baseline.
</next_action>