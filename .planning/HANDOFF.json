{
  "version": "1.0",
  "timestamp": "2026-09-16T19:47:14Z",
  "phase": null,
  "phase_name": null,
  "phase_dir": null,
  "plan": null,
  "task": null,
  "total_tasks": null,
  "status": "paused",
  "context": "STT-engine swap (sherpa-onnx + Groq Whisper) then omi replication into Ra as additive V2",
  "completed_tasks": [
    {"id": 1, "name": "Root-cause mic silence on built exe (device selection, not thresholds); picker fixed + sd[1] live verified", "status": "done", "notes": "debug log archived to Temp\\opencode\\ra_stt_debug_run1.log; exe REBUILT at hud\\dist\\Ra.exe (00:47) but user reports it still hears wrong/annoyingly"},
    {"id": 2, "name": "STT engine research: sherpa-onnx 1.13.8 chosen (parakeet-tdt-0.6b-v2-int8, cc-by-4.0, streaming transducer, feature_dim 80, 16kHz) + Groq Whisper optional cloud final", "status": "done", "notes": "API confirmed via introspection"},
    {"id": 3, "name": "Install sherpa-onnx into dev interpreter (pythoncore-3.14-64) - SUCCESS 1.13.8 + sherpa-onnx-core", "status": "done", "notes": "not yet installed into Store 3.13 build interpreter"},
    {"id": 4, "name": "Download parakeet model to ~/.ra/models", "status": "in_progress", "progress": "download ABORTED by user; partial file sherpa-onnx-nemo-parakeet-tdt-0.6b-v2-int8.tar.bz2 ~79MB (needs ~750MB) - DELETE and re-download"},
    {"id": 5, "name": "Clone omi repo for reuse study", "status": "done", "progress": "blobless+sparse clone at Temp\\opencode\\omi; sparse dirs: app backend mcp web desktop plugins sdks config contracts"},
    {"id": 6, "name": "Omi capability inventory + mapping to Ra (add/replace/keep)", "status": "not_started", "notes": "trees listed; deep file reads not yet done"},
    {"id": 10, "name": "REPO SPLIT: Ra frozen as pre-omi baseline; Ra-omi worktree created", "status": "done", "notes": "this repo committed at feff3ce (STT/device fixes); Ra-omi at C:\\Users\\mdahm\\OneDrive\\Documents\\Ra-omi with own git (baseline 91d6eab + INIT.md e71b8e9); all omi work goes there"},
    {"id": 7, "name": "Implement STT engine layer in config.py + audio_io.py (sherpa default, vosk fallback, groq final)", "status": "not_started", "notes": "design decided: _SherpaStream modeled on MicSession, gate preserved, models next to exe for build"},
    {"id": 8, "name": "Tests (pytest on Store 3.13) + live capture verify + bundle sherpa into spec + rebuild exe + user by-ear verify", "status": "not_started"},
    {"id": 9, "name": "Omi V2 architecture + pillars B1 memory / B2 apps / B3 personas / B4 mcp / B6 HUD + tests/rebuild/verify", "status": "not_started"}
  ],
  "remaining_tasks": [
    {"id": 4, "name": "Re-download parakeet model (~750MB) to ~/.ra/models", "status": "blocked", "notes": "user aborted first attempt"},
    {"id": 6, "name": "Omi capability inventory + mapping", "status": "not_started"},
    {"id": 7, "name": "Implement STT engine layer (config + audio_io)", "status": "not_started"},
    {"id": 8, "name": "Tests + live verify + bundle + rebuild exe + by-ear verify", "status": "not_started"},
    {"id": 9, "name": "Omi pillars B1-B6 + HUD + verify", "status": "not_started"}
  ],
  "blockers": [
    {"description": "Built Ra.exe STILL hears wrong after device fix: debug log shows sd[1] picked but stream blocks were digital silence (lvl=0.0000) during the run - capture-stability issue unresolved on the frozen exe", "type": "technical", "workaround": "re-examine ra_stt_debug_run1.log; re-verify after sherpa rebuild; reproduce live in dev (pythoncore 3.14) not Store python"},
    {"description": "Parakeet model download incomplete (~79MB partial vs ~750MB full)", "type": "external", "workaround": "delete partial file, re-download with long timeout; network attention needed"},
    {"description": "Task/subagent tool failing (NOT NULL constraint failed: session_message.seq) - happened 2x", "type": "technical", "workaround": "do research inline in main context (worked: clone+read+grep); retry subagents only if transient"}
  ],
  "human_actions_pending": [
    {"action": "Allow ~750MB parakeet model download (was aborted)", "context": "required for sherpa STT engine dev + bundling", "blocking": true},
    {"action": "By-ear verification of final exe (normal speech heard, screams heard, fan ignored, both modes)", "context": "user is the acceptance gate; do NOT claim fixed without it", "blocking": true},
    {"action": "Decide omi apps port strategy: omi chat-tools are Node (plugins/omi-*-app); port pattern into Ra's Python plugin system vs bridge", "context": "design-time decision for pillar B2", "blocking": false}
  ],
  "decisions": [
    {"decision": "sherpa-onnx becomes new default local STT (STT_ENGINE=sherpa-onnx default), vosk stays as explicit fallback, Groq Whisper (whisper-large-v3-turbo via RA_GROQ_API_KEY) becomes optional cloud final (STT_FINAL_ENGINE=groq); local whisper final still an option", "rationale": "user directive: best free local as default, deepgram-alternative optional; parakeet int8 far more accurate than vosk-small + has punctuation/caps", "phase": null},
    {"decision": "Keep Ra's noise classifier / gate (fan-proof, scream-proof) untouched; sherpa consumes the same gated pipeline as vosk did - phrase boundary via sherpa endpoint rules, keep sustained-speech + _is_human_audio backstops", "rationale": "do not mess up what works (cooler rejection, scream acceptance)", "phase": null},
    {"decision": "Model placement: dev uses ~/.ra/models; release bundles model NEXT TO exe (hud/dist/models/...) rather than inside Ra.exe (keeps 345MB exe from ballooning to ~1.1GB)", "rationale": "PyInstaller onefile size/launch cost", "phase": null},
    {"decision": "Omi = additive V2 layer in Ra (src/ra/omi/), replace only where omi is strictly better; pillars B1 memory, B2 apps/chat-tools, B3 personas, B4 mcp (omi mcp/ has own SDK), B6 HUD", "rationale": "user: keep Ra as it is, add as new version", "phase": null},
    {"decision": "REPO SPLIT: this Ra folder stays the frozen PRE-OMI baseline (voice continuous-convo fix + STT/device work, commit feff3ce). Omi integration is built in a SEPARATE folder C:\\Users\\mdahm\\OneDrive\\Documents\\Ra-omi (own git repo, baseline 91d6eab, INIT.md). STT engine swap continues HERE as Ra's own fix, then sync into Ra-omi; no omi code ever touches the Ra baseline", "rationale": "user: 'let everything here be as it was before omi, start omi in another folder containing Ra at the voice-continuous-convo-fixed state'", "phase": null},
    {"decision": "Exe rebuild requires: kill Ra processes first; build_exe.bat copies project .env -> ~/.ra/.env (so RA_STT_DEBUG etc survive)", "rationale": "learned earlier: locked-exe errors, env propagation", "phase": null}
  ],
  "uncommitted_files": [
    ".planning/HANDOFF.json + .planning/.continue-here.md (WIP handoff update for repo split)",
    "WORKING TREE IS COMMITTED: baseline fixes land in feff3ce; rest clean"
  ],
  "next_action": "Delete partial ~/.ra/models/sherpa-onnx-nemo-parakeet-tdt-0.6b-v2-int8.tar.bz2, re-download full (~750MB), then add sherpa/Groq constants to config.py and implement _SherpaStream + dispatch + listen_once in audio_io.py; run pytest; install sherpa-onnx into Store 3.13; rebuild exe; user verifies by ear",
  "context_notes": "Two-track work: (A) finish STT engine swap so user hears correctly, (B) then replicate omi. Subagent infra broken - research inline. Omi clone is blobless+sparse; do not add docs/ to sparse set (timed out). sherpa endpoint rules are INLINE in OnlineRecognizer.from_transducer (no EndpointConfig class). Dev interpreter = pythoncore-3.14-64 (mic-capable, now has sherpa-onnx 1.13.8); build/test = Store 3.13 (AppContainer, mic-blocked, needs sherpa-onnx pip install before rebuild)."
}